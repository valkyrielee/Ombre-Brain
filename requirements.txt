# ============================================================
# Ombre Brain Python 依赖
# 安装: pip install -r requirements.txt
# ============================================================

# MCP 协议 SDK（Claude 通信核心）
# 上界是必须的：mcp 2.x 把 FastMCP 改名成 MCPServer（server.py:57 的
# `from mcp.server.fastmcp import FastMCP` 在 2.x 下直接 ModuleNotFoundError）。
# 2026-08-29：一次「只改环境变量」的重新部署让 pip 拉到 2.1.1，容器 import 即死、
# CrashLoopBackOff。升到 2.x 要改代码，不是放开这个上界就行。
mcp>=1.0.0,<2

# 模糊匹配（记忆桶搜索）
rapidfuzz>=3.0.0

# 繁简归一（只在检索打分时折叠，不改动任何桶文件）
# 纯 Python、无 C 扩展、无外部数据文件，容器里装得上
zhconv>=1.4.3

# OpenAI 兼容客户端（支持 DeepSeek/Ollama/LM Studio/vLLM/Gemini 等任意兼容 API）
openai>=1.0.0

# YAML 配置解析
pyyaml>=6.0

# Markdown frontmatter 解析（桶文件读写）
python-frontmatter>=1.1.0

# 中文分词
jieba>=0.42.1

# 异步 HTTP 客户端（应用层保活 ping）
httpx>=0.27.0

# 向量相似度计算 (导入模式/聚类)
numpy>=1.24.0
scikit-learn>=1.2.0
